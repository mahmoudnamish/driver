import 'package:flutter/material.dart';

class DelaitesScreen extends StatelessWidget {

  const DelaitesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(),);
  }
}
